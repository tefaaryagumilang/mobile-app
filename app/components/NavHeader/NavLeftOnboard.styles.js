import {theme} from '../../styles/core.styles';

export default {
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingLeft: 20
  },
  arrow: {
    color: theme.brand,
    transform: [{rotate: '180deg'}]
  },
};